# 영어 단어장 학습 시스템 — Claude Code 작업 지침

이 저장소는 **동일한 레이아웃의 낱말카드/퀴즈를 단어장마다 찍어내기 위한 템플릿**입니다.
사용자가 단어 목록을 주면, 너(Claude Code)는 그 목록을 **새 단원 폴더의 `data.js`** 로만 변환하면 된다.
**HTML/CSS/JS 엔진 파일은 절대 수정하지 마라.** 레이아웃과 기능은 이미 완성되어 있고, 매번 동일하게 재현되어야 한다.

---

## 1. 파일 구조와 역할

| 파일 | 역할 | 수정 여부 |
|---|---|---|
| `index.html` | 시작(허브) 페이지 | ❌ 건드리지 않음 |
| `flashcards.html` | 낱말카드 엔진 | ❌ 건드리지 않음 |
| `quiz.html` | 퀴즈 엔진 | ❌ 건드리지 않음 |
| `data.js` | **단어 목록 + 설정** | ✅ 이것만 생성/교체 |

세 엔진 파일과 `data.js`는 **항상 같은 폴더**에 있어야 작동한다(`<script src="data.js">` 의존).

### 새 단원을 만드는 방법
사용자가 단어 목록을 주면:
1. `units/<단원-슬러그>/` 폴더를 새로 만든다. (슬러그는 영문 소문자-하이픈, 예: `inside-out-4`)
2. 루트의 `index.html`, `flashcards.html`, `quiz.html` **3개를 그 폴더로 복사**한다. (내용 변경 없이 그대로)
3. 그 폴더 안에 아래 스키마대로 **`data.js`만 새로 작성**한다.
4. 완성된 `units/<슬러그>/` 폴더 하나가 그 단원의 독립적인 학습 사이트가 된다(그대로 호스팅/공유 가능).

---

## 2. `data.js` 스키마 (반드시 이 형식)

```js
window.VOCAB_DATA = {
  title:    "단원 제목",              // 예: "Inside Out & Back Again"
  subtitle: "범위/부제",              // 예: "'Third Rule' ~ 'Most Relieved Day'"
  brand:    "SueSooEnglish",          // 고정
  theme: {                            // 단원별 색상(생략 시 기본 퍼플)
    primary: "#360F5A", bg: "#11041D", accent: "#A64B3D",
    button: "#EBD8F7", buttonHover: "#D4B6E6"
  },
  words: [
    { word: "표제어", part: "품사", meaning: "한글 뜻", example: "영어 예문", inflected: "" },
    // ...
  ]
};
```

### 각 필드 작성 규칙
- **word**: 표제어(원형). 소문자 기본, 고유명사만 대문자.
- **part**: 품사 약어 — `n.` `v.` `adj.` `adv.` `prep.` `conj.` `pron.` `phr.` 등.
- **meaning**: **한글** 뜻. 여러 뜻은 쉼표로. (예: `"뻗다, 길어지다"`)
- **example**: 그 단어가 **실제로 포함된** 영어 예문. 가능하면 원작 텍스트에서 인용, 없으면 단어를 자연스럽게 쓴 짧은 문장을 새로 만든다. ⚠️ 예문에 표제어(또는 그 활용형)가 **반드시 들어가야** 퀴즈 빈칸이 생성된다.
- **inflected** (선택): 예문에서 표제어가 **불규칙 활용**으로 쓰인 경우에만 그 형태를 적는다.
  - 규칙 변화(`extend→extending`, `shatter→shatters`, `pity→pities`)는 **자동 인식**되므로 비워둔다.
  - 불규칙(`go→went`, `buy→bought`, `run→ran`)이 예문에 쓰였으면 `inflected: "went"` 처럼 지정한다.

### 입력이 지저분할 때
사용자가 주는 목록은 형식이 제각각일 수 있다(엑셀 붙여넣기, "단어 - 뜻" 나열 등).
- 뜻이 없으면 사전적 의미를 채운다. 품사가 없으면 추론한다.
- 예문이 없으면 위 규칙대로 예문을 만든다.
- 애매한 항목은 임의로 지어내지 말고, 작업 후 사용자에게 짧게 확인한다.

---

## 3. 시스템 동작(참고 — 코드 수정 금지, 맥락 이해용)

- **낱말카드**: 3D 플립, 한/영 모드, TTS(원어민 음성 자동 선택), 편집·순서·위치 자동 저장(localStorage), 초기화.
- **퀴즈**: 3유형 자동 생성 —
  - `객관식 빈칸`: 예문 활용형이 정답/보기
  - `뜻 맞추기`: **원형**이 정답/보기 (활용형 아님)
  - `주관식 빈칸`: 예문에 영어로 직접 입력
- **오답 복습 + '틀린 것만 다시 풀기'(연습용)** 제공.
- **인증 규칙**: `index → quiz`를 **전체 단어장 처음부터 끝까지 + 한 번의 시도에서 만점**일 때만 `📸 인증 화면`이 뜬다. '틀린 것만 풀기'로 만점을 받아도 인증되지 않는다. (엔진에 이미 구현됨)

---

## 4. 작업 체크리스트 (새 단원 생성 시)
- [ ] `units/<슬러그>/` 폴더 생성 + 엔진 3파일 복사
- [ ] `data.js`의 `title`/`subtitle` 채움
- [ ] 모든 word에 `part`, `meaning`(한글), `example`(표제어 포함) 존재
- [ ] 불규칙 활용만 `inflected` 지정, 규칙은 비움
- [ ] 애매하게 처리한 항목이 있으면 사용자에게 확인
